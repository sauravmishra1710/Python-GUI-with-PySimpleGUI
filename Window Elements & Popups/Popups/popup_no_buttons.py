import PySimpleGUI as sg

sg.popup_no_buttons('Popup with only message. NO BUTTONS', title='No Buttons', grab_anywhere=True)

# The option 'grab_anywhere' allows to hold / grab the popup anywhere within the bounds and move / drag around the screen.